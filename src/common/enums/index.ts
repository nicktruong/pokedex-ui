export enum Gender {
  MALE = "MALE",
  OTHER = "OTHER",
  FEMALE = "FEMALE",
}

export enum UserRole {
  ADMIN = "ADMIN",
  CUSTOMER = "CUSTOMER",
}

export enum StorageKey {
  AccessToken = "AccessToken",
  RefreshToken = "RefreshToken",
}
